import type { Metadata } from 'next';
import './globals.css';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

export const metadata: Metadata = {
  title: 'Tribe Africa Foundation - Bringing Hope to the African Child',
  description: 'A non-governmental organization dedicated to education, poverty elimination, and skills development across Africa.',
  keywords: 'charity, africa, education, poverty elimination, NGO, Ghana, skills development',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="font-sans">
        <Header />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
